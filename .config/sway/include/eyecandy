# Sway Configuration
# Defines visual settings
#

# Font for window titles
	font pango:Roboto Regular 9


# Gaps
	gaps inner 5 
	gaps outer 4

# Set Edge Border Colors
	client.focused		$focused_element	$focused_background	#000000	$focused_border			$focused_border
	client.focused_inactive $unfocused_element	$unfocused_background	#000000	$focused_inactive_border	$focused_inactive_border
	client.unfocused	$unfocused_element	$unfocused_background	#000000	$unfocused_border		$unfocused_border

# Edge Borders (X and Wayland)
	for_window [class="^.*"] border pixel $edge_width
	for_window [app_id="^.*"] border pixel $edge_width
